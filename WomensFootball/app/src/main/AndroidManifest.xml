<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.dio.WomensFootball">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="me.dio.WomensFootball.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_soccer_player"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_soccer_player"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoccerNews">
        <activity
            android:name="me.dio.WomensFootball.ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>